apiVersion: v1
kind: Service
metadata:
  name: legal-ai-portal
  labels:
    app.kubernetes.io/name: legal-ai-portal
    app.kubernetes.io/component: frontend
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: legal-ai-portal
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30088
